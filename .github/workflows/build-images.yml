# name: Build & Publish Docker Images

# on:
#   workflow_dispatch:
#   push:
#     branches:
#       - v2

# jobs:
#   #

#   # # # # # # # # # # # # # # # # # # # #
#   # # # # # # # # # # # # # # # # # # # #

#   nginx:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       packages: write

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@main

#       - name: Cache turbo build setup
#         uses: actions/cache@v4
#         with:
#           path: .turbo
#           key: ${{ runner.os }}-turbo-${{ github.sha }}
#           restore-keys: |
#             ${{ runner.os }}-turbo-

#       - name: Login to GitHub Container Registry
#         uses: docker/login-action@master
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Build and Push Docker Image
#         uses: docker/build-push-action@master
#         with:
#           push: true
#           context: .
#           file: ./apps/nginx/Dockerfile
#           tags: ghcr.io/carrismetropolitana/api-navegante-tempo-real-nginx:${{ github.ref_name }}

#   # # # # # # # # # # # # # # # # # # # #
#   # # # # # # # # # # # # # # # # # # # #

#   server:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       packages: write

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@main

#       - name: Cache turbo build setup
#         uses: actions/cache@v4
#         with:
#           path: .turbo
#           key: ${{ runner.os }}-turbo-${{ github.sha }}
#           restore-keys: |
#             ${{ runner.os }}-turbo-

#       - name: Login to GitHub Container Registry
#         uses: docker/login-action@master
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Build and Push Docker Image
#         uses: docker/build-push-action@master
#         with:
#           push: true
#           context: .
#           file: ./apps/server/Dockerfile
#           tags: ghcr.io/carrismetropolitana/api-navegante-tempo-real-server:${{ github.ref_name }}

#   # # # # # # # # # # # # # # # # # # # # #
#   # # # # # # # # # # # # # # # # # # # # #

#   sync-gtfs:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       packages: write

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@main

#       - name: Cache turbo build setup
#         uses: actions/cache@v4
#         with:
#           path: .turbo
#           key: ${{ runner.os }}-turbo-${{ github.sha }}
#           restore-keys: |
#             ${{ runner.os }}-turbo-

#       - name: Login to GitHub Container Registry
#         uses: docker/login-action@master
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Build and Push Docker Image
#         uses: docker/build-push-action@master
#         with:
#           push: true
#           context: .
#           file: ./apps/sync-gtfs/Dockerfile
#           tags: ghcr.io/carrismetropolitana/api-sync-gtfs:${{ github.ref_name }}

